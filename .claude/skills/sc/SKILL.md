---
name: sc
description: "探针：检查 Stitch MCP 密钥及连接状态，如果有效则读取并总结当前 Stitch 画布/界面的内容。"
user-invocable: true
---

# 任务目标：探测 Stitch MCP 连接状态与界面的快照读取
你现在的角色是“MCP 运维与审查员”。请执行以下步骤来测试 Stitch MCP 的连通性并获取视觉上下文：

## ✅ 执行步骤
1. **工具自检**：检索当前环境中由 Stitch MCP 提供的所有可用工具（Tools），寻找与“获取画布、读取项目、获取当前代码或状态”相关的 API。
2. **连通性 Ping 测试**：调用最基础的查询 API 测试 MCP Key 的有效性。
3. **分发汇报**：
   - 🔴 **如果调用失败/未授权**：立即停止。向我报告：“❌ **Stitch MCP 密钥已失效或网络连接失败**”，并附上简要的错误日志（Error Message），不用做多余的解释。
   - 🟢 **如果调用成功**：向我报告：“✅ **Stitch MCP 连接正常，密钥有效！**”
4. **解析界面内容 (核心)**：在确认连接有效后，调用读取画布/内容的接口。请用结构化的 Markdown 列表向我总结当前 Stitch 界面里的内容。
   - 画布上目前有什么模块/组件？
   - 整体是什么配色、排版或风格？
   - (如果有代码返回) 请告诉我它的根 HTML 标签或核心库依赖是什么，但**不要直接打印冗长的完整代码**，只需提供摘要。

**验收标准**：反馈必须极度精简，先报状态（红/绿），再给界面摘要。